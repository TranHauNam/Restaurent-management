import { Stack } from "expo-router";

export default function BookedDetailLayout() {
    return (
        <Stack
            screenOptions={{
                headerShown: false,
                // headerTitle: "Payment Detail",
                // headerBackTitle: "Back"
            }}
        />
    );
}